# ============================================================
# requirements.txt
# Corrosion RC Beam Optimizer
# Tested on Python 3.10 / 3.11
# Install: pip install -r requirements.txt
# ============================================================

# ── Core Data Science ───────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3

# ── Deep Learning / Neural Network ──────────────────────────
# scikit-learn MLPRegressor is used (no PyTorch/TF dependency)
# Uncomment below if switching to PyTorch MLP:
# torch>=2.1
# torchvision>=0.16

# ── Ensemble ML Models ──────────────────────────────────────
xgboost>=2.0
catboost>=1.2               # Same algorithm Zhang et al. (2025) used
optuna>=3.5                  # Auto hyperparameter tuning

# ── Genetic Algorithm ───────────────────────────────────────
pymoo>=0.6.1

# ── Symbolic Regression ─────────────────────────────────────
pysr>=0.18.0
# Julia is required for PySR (auto-installed on first run)
# https://julialang.org/downloads/

# ── Explainability ──────────────────────────────────────────
shap>=0.44

# ── Visualisation ───────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18
kaleido>=0.2.1          # Plotly static image export

# ── Streamlit App ───────────────────────────────────────────
streamlit>=1.32

# ── PDF Report Generation ───────────────────────────────────
reportlab>=4.1

# ── Logging ─────────────────────────────────────────────────
loguru>=0.7

# ── Statistical Tests ───────────────────────────────────────
# scipy (already listed above) covers Wilcoxon, Chi2, etc.
pingouin>=0.5.4         # additional effect size helpers

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.66
python-dotenv>=1.0
click>=8.1

# ── Development & Testing ───────────────────────────────────
pytest>=8.0
pytest-cov>=4.1
black>=24.0
isort>=5.13
flake8>=7.0
pre-commit>=3.7
